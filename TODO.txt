
Server list:
- Show # of new messages

About dialog:
- ... ?

Message List:
- Safe area layout for table headers (iPhone XS Max in Landscape)
- Show message if no topics are configured and no messages present.

Action Menu:
- Show message if no actions are configured.

General:
- Allow "copy" of non-editable settings (server, topic, ...)
- Server status not always up-to-date (switch flight mode on/off)

Other:
- Set badge count to total # of new messages
- Handle multiple IP addresses in DNS lookup
- Server certificate checks: show error, ask user, ...
